# Lookup Problems

> Store information now, use it later.

---

## Core Idea

Instead of searching repeatedly:

```text
Store

↓

Lookup
```

---

## Recognition Signals

* Two Sum
* Complement
* Previous occurrence
* Fast lookup
* Have I seen this before?

---

## Mental Model

```text
Current Value

↓

Needed Value?

↓

Check HashMap
```

---

## Generic Template

```java
HashMap<Integer,Integer> map =
        new HashMap<>();

for(int i=0;i<n;i++) {

    if(map.containsKey(target-nums[i])) {

        // answer found
    }

    map.put(nums[i],i);
}
```

---

## Example Problems

### LC1 - Two Sum

```java
class Solution {

    public int[] twoSum(
            int[] nums,
            int target) {

        HashMap<Integer,Integer> map =
                new HashMap<>();

        for(int i=0;i<nums.length;i++) {

            int need =
                    target-nums[i];

            if(map.containsKey(need))
                return new int[]{
                    map.get(need),i
                };

            map.put(nums[i],i);
        }

        return new int[]{};
    }
}
```

Time: O(N)

Space: O(N)

---

### LC219 - Contains Duplicate II

Idea:

Store latest index.

```java
class Solution {

    public boolean containsNearbyDuplicate(
            int[] nums,
            int k) {

        HashMap<Integer,Integer> map =
                new HashMap<>();

        for(int i=0;i<nums.length;i++) {

            if(map.containsKey(nums[i]) &&
               i-map.get(nums[i])<=k)
                return true;

            map.put(nums[i],i);
        }

        return false;
    }
}
```

Time: O(N)

Space: O(N)

---

## One-Line Revision

```text
Lookup Problems =
Store now,
use later.
```
