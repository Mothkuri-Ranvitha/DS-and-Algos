# 09 - Infinite Array / Unknown Size Array

## Variation Definition

In normal Binary Search:

```text id="1q77hh"
Array length is known.
```

We know:

```java id="lbb7vl"
left = 0

right = n-1
```

and directly start Binary Search.

---

In this variation:

```text id="my3fz8"
Array length is unknown.
```

or

```text id="2xg8lw"
Array behaves like an infinite stream.
```

We do not know:

```java id="mh4ow4"
n
```

Therefore:

```java id="b7n5rp"
right = n-1
```

cannot be calculated.

---

# Core Idea

Before Binary Search:

```text id="bzj9tb"
Find a valid search window.
```

Then:

```text id="em0czk"
Perform Binary Search
inside that window.
```

---

# Recognition Signals

Think Infinite Array Pattern when you see:

* Unknown Length
* Infinite Sorted Array
* ArrayReader API
* Cannot access array size
* Sorted Stream

Keywords:

```text id="jl7v3r"
Infinite

Unknown Size

ArrayReader

Sorted Stream
```

---

# Core Technique

## Step 1

Expand search window.

Start:

```java id="qvdlru"
left = 0

right = 1
```

---

Check:

```java id="k9nn8e"
target > arr[right]
```

If yes:

Expand.

```java id="2d4l5s"
right = right * 2
```

---

Sequence:

```text id="a2g6vu"
1

2

4

8

16

32

64

128
```

---

Eventually:

```text id="8mwy3q"
target becomes
inside window.
```

---

## Step 2

Apply Binary Search.

---

# Why Doubling?

Suppose target is at:

```java id="0qz4ee"
index = 1000
```

Linear expansion:

```text id="97eygo"
1
2
3
4
5
...
1000
```

takes:

```text id="xd5f4m"
O(N)
```

---

Doubling:

```text id="s5z3kj"
1

2

4

8

16

32

64

128

256

512

1024
```

takes:

```text id="c0q7vi"
O(log N)
```

---

# Problem 1

# LC702 - Search in a Sorted Array of Unknown Size

## Problem Description

We cannot access:

```java id="o5zcbf"
nums.length
```

Instead:

```java id="6xg8y8"
reader.get(index)
```

is provided.

Find:

```java id="s7i7w7"
target
```

index.

---

## Example

Array:

```java id="k7djlwm"
[1,3,5,7,9,11,13,15]
```

Target:

```java id="6rqx87"
11
```

Answer:

```java id="z0zqjt"
5
```

---

# Intuition

Normal Binary Search needs:

```java id="xk2r9u"
left

right
```

But:

```java id="7vf8je"
right
```

is unknown.

---

So first find a valid range.

---

Start

```java id="v0ijv8"
left = 0

right = 1
```

---

Check:

```java id="a4j5vx"
reader.get(right)
```

If target is larger:

Expand.

---

Eventually:

```java id="gxej7e"
reader.get(right)
>=
target
```

Now target must lie inside:

```java id="n9b3d0"
[left,right]
```

---

Apply Binary Search.

---

# Dry Run

Target:

```java id="k75qur"
11
```

Array:

```java id="r5hmd6"
1 3 5 7 9 11 13 15
```

---

Initial

```java id="86l5l3"
left=0

right=1
```

```java id="o4mjlwm"
reader.get(1)=3
```

Expand.

---

```java id="ltg4k0"
left=2

right=4
```

```java id="tchj9k"
reader.get(4)=9
```

Still smaller.

Expand.

---

```java id="lr9frg"
left=5

right=8
```

Now:

```java id="yvdvwm"
reader.get(8)
```

is larger.

Target is guaranteed inside.

Apply Binary Search.

---

Find:

```java id="vlapjd"
index = 5
```

---

# Code

```java id="39g5je"
class Solution {

    public int search(
            ArrayReader reader,
            int target) {

        int left = 0;
        int right = 1;

        while(reader.get(right)
                < target){

            left = right + 1;
            right = right * 2;
        }

        while(left <= right){

            int mid =
                left + (right-left)/2;

            int value =
                reader.get(mid);

            if(value == target)
                return mid;

            if(value < target)
                left = mid + 1;
            else
                right = mid - 1;
        }

        return -1;
    }
}
```

---

# Complexity

Window Expansion:

```text id="u0jzbs"
O(log N)
```

Binary Search:

```text id="h4gq1h"
O(log N)
```

Total:

```text id="c4mh0u"
O(log N)
```

Space:

```text id="dz1w06"
O(1)
```

---

# Problem 2

# Find First Positive Number in Infinite Sorted Array

## Problem

Infinite array:

```java id="xg1vsz"
[-20,-10,-5,-2,-1,0,3,5,8...]
```

Find first positive number.

---

# Intuition

Target condition:

```java id="4z5r12"
arr[i] > 0
```

We need first occurrence.

---

## Step 1

Expand window until:

```java id="mkjlwm"
arr[right] > 0
```

---

## Step 2

Binary Search first positive.

---

# Code

```java id="9apg68"
int firstPositive(ArrayReader reader){

    int left = 0;
    int right = 1;

    while(reader.get(right) <= 0){

        left = right + 1;
        right *= 2;
    }

    int ans = -1;

    while(left <= right){

        int mid =
            left + (right-left)/2;

        if(reader.get(mid) > 0){

            ans = mid;
            right = mid - 1;
        }
        else{

            left = mid + 1;
        }
    }

    return ans;
}
```

---

# Complexity

```text id="eyqlf5"
Time  : O(log N)

Space : O(1)
```

---

# Pattern Summary

| Problem        | Core Idea                       |
| -------------- | ------------------------------- |
| LC702          | Expand Window + Binary Search   |
| First Positive | Expand Window + Boundary Search |

---

# Universal Template

```java id="px9rua"
left = 0;
right = 1;

while(condition not satisfied){

    left = right + 1;
    right *= 2;
}

Binary Search(left,right);
```

---

# Interview Checklist

Whenever you see:

```text id="ihjlwm"
Unknown Size

Infinite Array

ArrayReader

Stream
```

Ask:

```text id="ltdd6u"
Can I first find
a valid window?
```

If yes:

```text id="30dv9i"
Expand Window

↓

Binary Search
```

---

# One-Line Revision

```text id="1a5m4z"
Infinite Array Pattern =
Find the range first,
then perform Binary Search.
```
# 10 - Bitonic Array Problems

## Variation Definition

A Bitonic Array is an array that:

```text id="vfwvwj"
Strictly Increases

Then

Strictly Decreases
```

Example:

```java id="tt7u9e"
1 3 8 12 4 2
```

Visual:

```text id="9z12gb"
       12
      /  \
     /    \
    /      \
1 3 8       4 2
```

---

# Core Observation

A Bitonic Array contains:

```text id="jpj7cs"
Increasing Part

Peak

Decreasing Part
```

Example:

```java id="4b0ayc"
1 3 8 12 4 2
```

Peak:

```java id="fjlwm7"
12
```

Index:

```java id="6mnfzy"
3
```

---

# Why Binary Search Works?

Observe:

```java id="6kj3c9"
nums[mid]
```

and

```java id="4e12zk"
nums[mid+1]
```

---

Case 1

```java id="q6baf1"
nums[mid] < nums[mid+1]
```

We are climbing.

```text id="86m9sq"
Peak lies right.
```

---

Case 2

```java id="0nyvjlwm"
nums[mid] > nums[mid+1]
```

We are descending.

```text id="yljlj2"
Peak lies left
including mid.
```

---

# Recognition Signals

Think Bitonic Pattern when you see:

* Bitonic Array
* Mountain Array
* Increasing then Decreasing
* Search in Mountain Array
* Maximum Element

Keywords:

```text id="f9mjlwm"
Bitonic

Mountain

Peak

Maximum

Increasing Then Decreasing
```

---

# Problems Covered

## Problem 1

Find Peak in Bitonic Array

Concept:

```text id="2jlwm5"
Peak Binary Search
```

---

## Problem 2

Search in Bitonic Array

Concept:

```text id="nr4z5u"
Peak
+
Two Binary Searches
```

---

# Problem 1

# Find Peak in Bitonic Array

## Problem Description

Given:

```java id="9d3iy6"
[1,3,8,12,4,2]
```

Find:

```java id="jlwm3d"
12
```

---

## Intuition

Peak divides the array into:

```text id="8d6lku"
Increasing

Peak

Decreasing
```

---

Compare:

```java id="x9l32t"
nums[mid]
```

with

```java id="sbjlwm"
nums[mid+1]
```

---

If:

```java id="8ujtww"
nums[mid] < nums[mid+1]
```

Peak lies right.

---

If:

```java id="q7tqpn"
nums[mid] > nums[mid+1]
```

Peak lies left.

---

# Dry Run

Input:

```java id="oqljlwm"
[1,3,8,12,4,2]
```

---

Iteration 1

```java id="8b1d9e"
left=0
right=5

mid=2
```

```java id="mjlwm8f"
nums[2]=8
nums[3]=12
```

```java id="ajij8e"
8 < 12
```

Move right.

```java id="m8p3xq"
left=3
```

---

Iteration 2

```java id="mjlwm2r"
mid=4
```

```java id="sxjlwm4"
nums[4]=4
nums[5]=2
```

Descending.

Move left.

```java id="h6pjlwm"
right=4
```

---

Iteration 3

```java id="jjlwm5m"
mid=3
```

Peak found.

---

## Code

```java
class Solution {

    public int findPeak(int[] nums) {

        int left = 0;
        int right = nums.length - 1;

        while(left < right){

            int mid =
                left + (right-left)/2;

            if(nums[mid] < nums[mid+1]){

                left = mid + 1;
            }
            else{

                right = mid;
            }
        }

        return nums[left];
    }
}
```

---

## Complexity

```text
Time  : O(log N)

Space : O(1)
```

---

# Problem 2

# Search in Bitonic Array

## Problem Description

Given:

```java
[1,3,8,12,4,2]
```

Target:

```java
4
```

Return:

```java
4
```

(index)

---

## Intuition

Bitonic array contains:

```text
Increasing Part

Peak

Decreasing Part
```

Example:

```java
1 3 8 12 4 2
      ^
```

Peak:

```java
12
```

---

Searching directly is difficult.

Instead:

### Step 1

Find Peak.

---

### Step 2

Binary Search Left Half.

```java
1 3 8 12
```

Ascending.

Normal Binary Search.

---

### Step 3

If not found:

Binary Search Right Half.

```java
12 4 2
```

Descending.

Reverse Binary Search.

---

# Dry Run

Target:

```java
4
```

Array:

```java
1 3 8 12 4 2
```

---

Peak:

```java
index=3
```

---

Search Left:

```java
1 3 8 12
```

Not found.

---

Search Right:

```java
12 4 2
```

Found:

```java
index=4
```

---

## Code

```java
class Solution {

    public int searchBitonic(
            int[] nums,
            int target) {

        int peak = findPeakIndex(nums);

        int left =
            binarySearchAsc(
                nums,
                0,
                peak,
                target);

        if(left != -1)
            return left;

        return binarySearchDesc(
                nums,
                peak + 1,
                nums.length - 1,
                target);
    }

    private int findPeakIndex(
            int[] nums){

        int left = 0;
        int right = nums.length - 1;

        while(left < right){

            int mid =
                left + (right-left)/2;

            if(nums[mid] < nums[mid+1])
                left = mid + 1;
            else
                right = mid;
        }

        return left;
    }

    private int binarySearchAsc(
            int[] nums,
            int left,
            int right,
            int target){

        while(left <= right){

            int mid =
                left + (right-left)/2;

            if(nums[mid] == target)
                return mid;

            if(nums[mid] < target)
                left = mid + 1;
            else
                right = mid - 1;
        }

        return -1;
    }

    private int binarySearchDesc(
            int[] nums,
            int left,
            int right,
            int target){

        while(left <= right){

            int mid =
                left + (right-left)/2;

            if(nums[mid] == target)
                return mid;

            if(nums[mid] > target)
                left = mid + 1;
            else
                right = mid - 1;
        }

        return -1;
    }
}
```

---

## Complexity

Find Peak:

```text
O(log N)
```

Ascending Search:

```text
O(log N)
```

Descending Search:

```text
O(log N)
```

Total:

```text
O(log N)
```

Space:

```text
O(1)
```

---

# Related LeetCode Problems

### LC852

Peak Index in Mountain Array

---

### LC162

Find Peak Element

---

### LC1095

Find in Mountain Array

(Most Important)

---

# Pattern Summary

| Problem        | Idea                       |
| -------------- | -------------------------- |
| Find Peak      | Compare mid and mid+1      |
| Search Bitonic | Peak + Two Binary Searches |
| LC1095         | Mountain Search            |

---

# Interview Checklist

Whenever you see:

```text
Increasing

Peak

Decreasing
```

Think:

```text
Find Peak

↓

Split Array

↓

Binary Search
```

---

# One-Line Revision

```text
Bitonic Array =
Find Peak First,
then Binary Search both sides.
```
# 11 - K-th Smallest / K-th Largest Problems

## Variation Definition

This is one of the most important advanced Binary Search patterns.

Unlike normal Binary Search:

```text id="8gm5hn"
Search Target
```

Here we search for:

```text id="v2b8g7"
K-th Smallest

K-th Largest

K-th Closest

K-th Distance
```

---

# Core Intuition

Suppose:

```java id="5e1zqy"
nums = [1,3,5,7,9]
```

Find:

```java id="l0o8e8"
3rd Smallest
```

Answer:

```java id="d6tjlwm"
5
```

---

Instead of searching indices:

We search values.

---

Suppose:

```java id="mjlwm4j"
mid = 5
```

Ask:

```text id="jlwm2p"
How many numbers
are <= 5 ?
```

Count:

```java id="jlwm7f"
1 3 5
```

Count:

```java id="jlwm9x"
3
```

---

If:

```text id="jlwm5t"
count >= k
```

Answer may be smaller.

Move left.

---

If:

```text id="jlwm6h"
count < k
```

Need larger value.

Move right.

---

# Universal Pattern

Search Space:

```text id="jlwm1m"
Minimum Value

↓

Maximum Value
```

---

Check:

```text id="jlwm0k"
How many elements
are <= mid ?
```

---

Pattern:

```text id="jlwm4v"
count < k

→ right side

count >= k

→ left side
```

---

# Recognition Signals

Think K-th Binary Search when you see:

* K-th Smallest
* K-th Largest
* K-th Distance
* K-th Pair
* K-th Fraction
* Rank Query

Keywords:

```text id="jlwm3r"
K-th

Rank

Order Statistic

Distance

Fraction
```

---

# Problems Covered

## LC378

K-th Smallest Element in Sorted Matrix

---

## LC668

K-th Smallest Number in Multiplication Table

---

## LC719

K-th Smallest Pair Distance

---

## LC786

K-th Smallest Prime Fraction

---

# Problem 1

# LC378 - K-th Smallest Element in Sorted Matrix

## Problem Description

Given:

```java id="jlwm2a"
1  5  9
10 11 13
12 13 15
```

k:

```java id="jlwm7w"
8
```

Output:

```java id="jlwm4g"
13
```

---

## Intuition

Search value range:

```java id="jlwm5z"
1 → 15
```

---

Suppose:

```java id="jlwm8b"
mid = 8
```

Count:

```text id="jlwm9u"
How many values
<= 8 ?
```

Count:

```java id="jlwm0e"
1 5
```

Only:

```java id="jlwm1n"
2
```

Need larger answer.

---

If count becomes:

```java id="jlwm3f"
8
```

Possible answer.

Try smaller.

---

## Code

```java id="jlwm6s"
class Solution {

    public int kthSmallest(
            int[][] matrix,
            int k) {

        int n = matrix.length;

        int left = matrix[0][0];
        int right =
            matrix[n-1][n-1];

        while(left < right){

            int mid =
                left + (right-left)/2;

            int count =
                countLessEqual(
                        matrix,
                        mid);

            if(count < k){

                left = mid + 1;
            }
            else{

                right = mid;
            }
        }

        return left;
    }

    private int countLessEqual(
            int[][] matrix,
            int target){

        int n = matrix.length;

        int row = n - 1;
        int col = 0;

        int count = 0;

        while(row >= 0
                && col < n){

            if(matrix[row][col]
                    <= target){

                count += row + 1;
                col++;
            }
            else{

                row--;
            }
        }

        return count;
    }
}
```

---

## Complexity

```text id="jlwm8q"
Time  : O(N log Range)

Space : O(1)
```

---

# Problem 2

# LC668 - K-th Smallest Number in Multiplication Table

## Problem Description

Table:

```java id="jlwm2x"
1 2 3

2 4 6

3 6 9
```

Find:

```java id="jlwm6v"
5th smallest
```

Output:

```java id="jlwm9l"
3
```

---

## Intuition

Search range:

```java id="jlwm1c"
1 → m*n
```

---

Suppose:

```java id="jlwm4b"
mid = 4
```

Count:

```text id="jlwm8m"
How many values
<= 4 ?
```

For row:

```java id="jlwm5q"
1
```

Count:

```java id="jlwm0r"
4
```

For row:

```java id="jlwm3u"
2
```

Count:

```java id="jlwm7i"
2
```

For row:

```java id="jlwm2k"
3
```

Count:

```java id="jlwm6o"
1
```

Total:

```java id="jlwm1v"
7
```

---

## Code

```java id="jlwm4n"
class Solution {

    public int findKthNumber(
            int m,
            int n,
            int k) {

        int left = 1;
        int right = m*n;

        while(left < right){

            int mid =
                left + (right-left)/2;

            int count = 0;

            for(int row=1;
                    row<=m;
                    row++){

                count +=
                    Math.min(
                        mid/row,
                        n);
            }

            if(count < k){

                left = mid + 1;
            }
            else{

                right = mid;
            }
        }

        return left;
    }
}
```

---

## Complexity

```text id="jlwm9d"
Time  : O(M log(M*N))

Space : O(1)
```

---

# Problem 3

# LC719 - K-th Smallest Pair Distance

## Problem Description

Given:

```java id="jlwm3y"
nums=[1,3,1]
```

Distances:

```java id="jlwm7n"
0

2

2
```

Find:

```java id="jlwm8c"
1st smallest
```

Answer:

```java id="jlwm5m"
0
```

---

## Intuition

Sort first.

```java id="jlwm2j"
1 1 3
```

Search distance:

```java id="jlwm0v"
0 → max-min
```

---

Suppose:

```java id="jlwm9g"
mid = 1
```

Count pairs:

```text id="jlwm6n"
distance <= 1
```

Use sliding window.

---

If count:

```text id="jlwm4j"
< k
```

Need larger distance.

---

Else:

```text id="jlwm8y"
move left
```

---

## Code

```java id="jlwm1w"
class Solution {

    public int smallestDistancePair(
            int[] nums,
            int k) {

        Arrays.sort(nums);

        int left = 0;

        int right =
            nums[nums.length-1]
            -
            nums[0];

        while(left < right){

            int mid =
                left + (right-left)/2;

            int count = 0;

            int start = 0;

            for(int end=0;
                    end<nums.length;
                    end++){

                while(nums[end]
                        -
                        nums[start]
                        > mid){

                    start++;
                }

                count +=
                    end-start;
            }

            if(count < k){

                left = mid + 1;
            }
            else{

                right = mid;
            }
        }

        return left;
    }
}
```

---

## Complexity

```text id="jlwm7z"
Time  : O(N log N + N log Range)

Space : O(1)
```

---

# Problem 4

# LC786 - K-th Smallest Prime Fraction

## Problem Description

Given:

```java id="jlwm5p"
[1,2,3,5]
```

Fractions:

```java id="jlwm3m"
1/2

1/3

1/5

2/3

2/5

3/5
```

Find:

```java id="jlwm1j"
k-th smallest
```

fraction.

---

## Intuition

Search answer:

```java id="jlwm6x"
0.0 → 1.0
```

---

Suppose:

```java id="jlwm8n"
mid = 0.5
```

Count:

```text id="jlwm2s"
fractions <= 0.5
```

---

If count:

```text id="jlwm0b"
< k
```

Need larger fraction.

---

Else:

```text id="jlwm9p"
move left
```

---

## Complexity

```text id="jlwm4u"
Time  : O(N log Precision)

Space : O(1)
```

---

# Pattern Summary

| Problem | Search Space          |
| ------- | --------------------- |
| LC378   | Matrix Values         |
| LC668   | Multiplication Values |
| LC719   | Distance              |
| LC786   | Fraction Value        |

---

# Universal Template

```java id="jlwm7r"
while(left < right){

    mid = ...

    count =
        countLessEqual(mid);

    if(count < k){

        left = mid + 1;
    }
    else{

        right = mid;
    }
}
```

---

# Interview Checklist

Whenever you see:

```text id="jlwm3h"
K-th Smallest

K-th Largest

Rank
```

Ask:

```text id="jlwm5k"
Can I count
how many values
are <= mid ?
```

If yes:

```text id="jlwm8h"
Binary Search
on Values
```

---

# One-Line Revision

```text id="jlwm6j"
K-th Problems =
Guess Answer,
Count <= Mid,
Shrink Search Space.
```
# Remaining Binary Search Variations

## Variation 12

# Median Problems

## Core Idea

Instead of searching values:

```text
Search Partition Position
```

---

## Recognition Signals

Keywords:

```text
Median

Middle Element

Two Sorted Arrays

Balanced Partition
```

---

## Most Important Problem

### LC4 - Median of Two Sorted Arrays

Difficulty:

```text
Hard
```

Interview Importance:

```text
★★★★★
```

---

## Core Intuition

Suppose:

```java
nums1 = [1,3]

nums2 = [2]
```

Combined:

```java
1 2 3
```

Median:

```java
2
```

---

Brute Force:

```text
Merge Arrays
```

Complexity:

```text
O(m+n)
```

---

Interview Solution:

```text
Binary Search on Partition
```

---

Goal:

Split arrays into:

```text
Left Half

=

Right Half
```

Example:

```java
1 | 3
2 |
```

Left:

```java
1 2
```

Right:

```java
3
```

Balanced.

---

Check:

```text
max(left)

<=

min(right)
```

If true:

Correct partition found.

---

Key Formula:

```java
leftMax <= rightMin
```

---

Complexity

```text
Time  : O(log(min(m,n)))

Space : O(1)
```

---

## Why Important?

This introduces a completely new Binary Search idea:

```text
Search Partition

NOT

Search Value
```

---

# Variation 13

# Advanced Binary Search on Answer

## Core Idea

Optimization Problems.

---

Recognition:

```text
Minimum Possible

Maximum Possible

Minimize Maximum

Maximize Minimum
```

---

# Universal Question

```text
Can Answer = X ?
```

---

If YES:

```text
Try Better Answer
```

---

If NO:

```text
Move Opposite Direction
```

---

This creates:

```text
FFFFFTTTTT
```

or

```text
TTTTTFFFFF
```

which Binary Search can exploit.

---

# Important Problems

## LC1231 Divide Chocolate

Goal:

```text
Maximize Minimum Sweetness
```

---

## LC1552 Magnetic Force Between Two Balls

Goal:

```text
Maximize Minimum Distance
```

---

## LC2187 Minimum Time to Complete Trips

Goal:

```text
Minimize Time
```

---

## LC1870 Minimum Speed to Arrive on Time

Goal:

```text
Minimize Speed
```

---

## LC2064 Minimized Maximum Products

Goal:

```text
Minimize Maximum Load
```

---

# Common Pattern

Suppose:

```text
Can finish in 10 days ?
```

YES

↓

Try 9 days

---

Suppose:

```text
Can finish in 5 days ?
```

NO

↓

Need larger answer

---

Pattern:

```text
Can(mid)
```

returns:

```text
true

or

false
```

---

Then:

```java
if(can(mid))
    right = mid;
else
    left = mid + 1;
```

---

# Final Binary Search Roadmap

```text
01 Classical Binary Search

02 First / Last Occurrence

03 Lower Bound / Upper Bound

04 Rotated Sorted Array

05 Binary Search on Answer

06 Peak / Mountain Array

07 Matrix Binary Search

08 Missing / Duplicate / Unique

09 Infinite Array

10 Bitonic Array

11 K-th Smallest / Largest

12 Median Problems

13 Advanced Binary Search on Answer
```

---

# One-Line Revision

```text
Variation 12
=
Binary Search on Partition

Variation 13
=
Binary Search on Feasibility
```
